import { FirstLetterCapitalizeDirective } from './first-letter-capitalize.directive';

describe('FirstLetterCapitalizeDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstLetterCapitalizeDirective();
    expect(directive).toBeTruthy();
  });
});
