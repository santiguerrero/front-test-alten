import { InitialsLettersNameDirective } from './initials-letters-name.directive';

describe('InitialsLettersNameDirective', () => {
  it('should create an instance', () => {
    const directive = new InitialsLettersNameDirective();
    expect(directive).toBeTruthy();
  });
});
